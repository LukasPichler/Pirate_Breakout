
import Phaser, { Scene } from 'phaser';

export class NameHolder extends Phaser.Scene {
    //JsonVariableNames
    public static levelsJsonName: string = 'levels';
    public static boxJsonName: string = 'boxesJson';
}